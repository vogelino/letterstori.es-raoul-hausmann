type Query {
  allEntities: [Entity]
  allDocuments: [Document]
  entity(id: String!): Entity
  document(id: String!): Document
  allStories: [Story]
  story(id: String!): Story
}

schema {
  query: Query
}
